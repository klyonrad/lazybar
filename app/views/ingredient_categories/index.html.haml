%p#notice= notice
%h1 Listing Ingredient Categories
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @ingredient_categories.each do |ingredient_category|
      %tr
        %td= ingredient_category.name
        %td= link_to 'Show', ingredient_category
        - if admin_signed_in?
          %td= link_to 'Edit', edit_ingredient_category_path(ingredient_category)
          %td= link_to 'Destroy', ingredient_category, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Ingredient category', new_ingredient_category_path
